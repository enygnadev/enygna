
// Este arquivo não é mais necessário com a abordagem client-only
// Todas as funcionalidades foram movidas para o client-side

export const adminAuth = null;
export const adminDb = null;

console.log('Firebase Admin SDK removido - usando apenas client SDK');
